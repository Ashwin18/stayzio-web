function switchAuthTab(tab){const buttons=document.querySelectorAll('.auth-tabs button');buttons.forEach((b,i)=>{b.classList.toggle('active',(tab==='otp'&&i===0)||(tab==='password'&&i===1));});const otp=document.getElementById('otpForm');const pass=document.getElementById('passwordForm');if(otp) otp.style.display=tab==='otp'?'block':'none';if(pass) pass.style.display=tab==='password'?'block':'none'}
function showOtpBox(){const panel=document.getElementById('otpArea');if(panel){panel.style.display='block';const first=panel.querySelector('input');if(first) first.focus();}}
function showSignupOtp(){const panel=document.getElementById('signupOtpArea');if(panel){panel.style.display='block';const first=panel.querySelector('input');if(first) first.focus();}}
document.addEventListener('input',function(e){if(e.target.closest('.otp-boxes')&&e.target.tagName==='INPUT'){const all=[...e.target.closest('.otp-boxes').querySelectorAll('input')];const idx=all.indexOf(e.target);e.target.value=e.target.value.replace(/\D/g,'').slice(0,1);if(e.target.value&&all[idx+1]) all[idx+1].focus();}});
document.addEventListener('keydown',function(e){if(e.target.closest('.otp-boxes')&&e.target.tagName==='INPUT'&&e.key==='Backspace'&&!e.target.value){const all=[...e.target.closest('.otp-boxes').querySelectorAll('input')];const idx=all.indexOf(e.target);if(all[idx-1]) all[idx-1].focus();}});
